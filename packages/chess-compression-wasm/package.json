{
  "name": "chess-compression-wasm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wasm-pack build ../chess-compression --target web --out-dir ../chess-compression-wasm/pkg --verbose",
    "test": "wasm-pack test --node ../chess-compression"
  },
  "files": [
    "pkg"
  ],
  "main": "pkg/chess_compression.js",
  "types": "pkg/chess_compression.d.ts"
}
